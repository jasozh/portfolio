/** @type {import("tailwindcss").Config} */
const config = {
  prefix: "tw-",

  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {},
  },

  plugins: [],
};

module.exports = config;
